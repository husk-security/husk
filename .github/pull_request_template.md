<!-- What changes and why. A reader a year from now has the diff and nothing else. -->

- [ ] `cargo test --no-default-features` passes
- [ ] `cargo clippy --no-default-features --all-targets -- -D warnings` is clean
- [ ] A detection change ships with a fixture under `tst/` and a test
- [ ] A CLI subcommand or MCP tool change updates `integrations/husk-agent-guide.md` and the Claude Code skill (they mirror each other)
